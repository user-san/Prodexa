1️⃣One-Time Setup (Initial Push):
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/user-san/Products_Listing.git
git push -u origin main

2️⃣For Hosting on GitHub Pages (Vite projects):
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d dist"
}

3️⃣in vite.config.js:
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  base: "/Products_Listing/", // your repo name
  plugins: [react()]
})






✅ After Making Changes (Daily Workflow):

git add .
git commit -m "Meaningful message about your update"
git push


📢 for host the page:

  ----- npm run deploy
